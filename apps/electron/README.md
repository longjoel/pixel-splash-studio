# Electron Target

This target hosts the full desktop build of Pixel Splash Studio.

Current status:
1. Electron main/preload sources remain in `electron/`.
2. Build output is generated at `dist-electron/` from the repository root.

Build:
```bash
npm run build:main
```
